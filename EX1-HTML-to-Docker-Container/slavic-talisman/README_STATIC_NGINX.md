# Nginx Static HTML Microproject

Этот микропроект демонстрирует простой Docker-образ на базе **nginx**, который отображает кастомную HTML‑страницу вместо стандартной страницы приветствия Nginx.

Проект полностью соответствует заданию:

- Используется официальный образ `nginx:alpine`
- Статические файлы размещаются в `/usr/share/nginx/html`
- Главная страница (`index.html`) заменяет стандартное приветствие Nginx
- Репозиторий содержит Dockerfile и все необходимые статичные файлы

---

## 📁 Структура проекта

```
/
├── Dockerfile
├── index.html
└── css/
    └── style.css   (опционально)
```

---

## 🐳 Dockerfile

```Dockerfile
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
```

---

## 🚀 Запуск контейнера

### 1. Сборка образа

```bash
docker build -t my-static-site .
```

### 2. Запуск

```bash
docker run --rm -d -p 8080:80 --name static-site my-static-site
```

Теперь сайт доступен по адресу:

👉 **http://localhost:8080**

---

## 🛑 Остановка контейнера

```bash
docker stop static-site
```

---

## 📌 Примечания

- Всё содержимое директории с Dockerfile копируется в `/usr/share/nginx/html` контейнера.
- Чтобы изменить страницу приветствия, достаточно отредактировать `index.html` и пересобрать образ.