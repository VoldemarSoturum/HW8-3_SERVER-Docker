# CОЗДАНИЕ КОНТЕЙНЕРА ДЛЯ REST API СЕРВЕРА DJANGO "CRUD: СКЛАДЫ И ЗАПАСЫ".

Этот проект представляет собой REST API сервер на Django/DRF, упакованный в Docker-контейнер.  
База данных переведена на **SQLite**, чтобы контейнер работал без PostgreSQL.

---

## 📌 Структура проекта

```
.
├── Dockerfile
├── EX1+ADDIONS-Stocks_products/
│   ├── manage.py
│   ├── requirements.txt
│   ├── stocks_products/
│   │   ├── settings.py (обновлён под SQLite + env)
│   │   └── ...
│   └── ...
└── README.md (этот файл)
```

---

## 🔧 Изменения в Django-проекте

### 1. Проект переведён на SQLite

В `stocks_products/settings.py` заменён блок базы данных:

```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": os.getenv("DJANGO_DB_NAME", BASE_DIR / "db.sqlite3"),
    }
}
```

### 2. Добавлена поддержка переменных окружения

Используются переменные:

| Переменная | Описание |
|-----------|----------|
| `DJANGO_SECRET_KEY` | Секретный ключ Django |
| `DJANGO_DEBUG` | 1 или 0 |
| `DJANGO_ALLOWED_HOSTS` | Список хостов через пробел |

---

## 🐳 Dockerfile

В корне репозитория находится Dockerfile, который собирает приложение без PostgreSQL.

---

## 🚀 Команды для запуска контейнера

### 1. Сборка Docker-образа

Выполнить в корне репозитория:

```bash
docker build -t stocks-products .
```

### 2. Запуск контейнера

```bash
docker run --rm -it -p 8000:8000 stocks-products
```

### 3. Запуск с переменными окружения

```bash
docker run --rm -it ^
  -p 8000:8000 ^
  -e DJANGO_DEBUG=0 ^
  -e "DJANGO_SECRET_KEY=super-secret-key" ^
  -e "DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1" ^
  stocks-products
```

(Команды для Windows PowerShell/CMD. В Linux/macOS заменить `^` на `\`.)

---

## 🧪 Проверка API (Postman / VS Code REST Client)

После запуска контейнера backend доступен по URL:

```
http://localhost:8000/
```

### Примеры запросов

#### Получить список товаров
```
GET http://localhost:8000/api/v1/products/
```

#### Создать товар
```
POST http://localhost:8000/api/v1/products/
Content-Type: application/json

{
  "title": "Test product",
  "description": "Some description"
}
```

#### Получить список складов
```
GET http://localhost:8000/api/v1/stocks/
```

---


